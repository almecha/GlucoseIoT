# — Virtual environments —
# If you use venv/ or .venv/, just keep these lines
venv/
.venv/
dashboard/
thingspeak_adaptor/

# In case the environment is located at the project root (bin/, lib/, include/, etc/, share/, pyvenv.cfg)
bin/
lib/
lib64/
include/
share/
etc/
pyvenv.cfg
# pip places this file in the root
pip-selfcheck.json

# — Python bytecode —
__pycache__/
*.py[cod]
*$py.class

# — Package build/distribution artifacts —
build/
dist/
*.egg-info/
.eggs/

# — Jupyter —
.ipynb_checkpoints/

# — IDE / editors —
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# — Logs and temporary files —
*.log
*.tmp

# — System artifacts —
.DS_Store
Thumbs.db
